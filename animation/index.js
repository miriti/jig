define(['./Alpha', './Color', './Move', './MoveTo', './Rotate', './RotateTo'], function(Alpha, Color, Move, MoveTo, Rotate, RotateTo) {
  return {
    Alpha: Alpha,
    Color: Color,
    Move: Move,
    MoveTo: MoveTo,
    Rotate: Rotate,
    RotateTo: RotateTo
  }
});
